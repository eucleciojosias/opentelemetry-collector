receivers:
  otlp:
    protocols:
      grpc:
        endpoint: '127.0.0.1:4317'
  hostmetrics:
    collection_interval: 1m
    # scrapers:
    #   load:
    #   memory:

processors:
  batch: null

exporters:
  logging:
    loglevel: info
  otlp/elastic:
    endpoint: "https://6857d246545a4f1bba390c8f41617f61.apm.us-east-2.aws.elastic-cloud.com:443"
    headers:
      Authorization: "Bearer 0O6wzclW8LuTRL4pSr"

service:
  pipelines:
    metrics:
      receivers:
        - otlp
        - hostmetrics
      exporters:
        - otlp/elastic
    traces:
      receivers:
        - otlp
      processors:
        - batch
      exporters:
        - otlp/elastic
